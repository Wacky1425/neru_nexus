# Flutter Cleanup 6

- Removed legacy Budget fields `savingTarget` / `dreamTarget`.
- Budget settings now manages salary, side income, fixed/variable budgets, and NISA target.
- Removed stale DreamCard source if it remained locally.
